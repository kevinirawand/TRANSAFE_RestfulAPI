import ProductService from '../../product/product-service';
import TransactionService from '../transaction-service';

class NegoService {
   public acceptNego = async (
      transaction_id: number,
      amount: number,
   ): Promise<void> => {
      
   };
}

export default new NegoService();
